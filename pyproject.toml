[project]
name = "nlp-final-proj"
version = "0.1.0"
description = "Multimodal RAG pipeline for coursework PDFs, OCR, images, and videos"
requires-python = ">=3.10,<3.13"

dependencies = []

[tool.uv]
package = false